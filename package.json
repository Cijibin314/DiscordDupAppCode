{
  "name": "frontend",
  "version": "1.0.0",
  "description": "A frontend app to allow users to commumicate similar to a messaging system",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . MyApp --platform=win32 --arch=x64 --out=out --overwrite"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "path": "^0.12.7"
  },
  "devDependencies": {
    "electron": "^31.3.1"
  }
}
